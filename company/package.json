{
  "name": "shinsung-homepage",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --minify --format=iife --target=es2019 --outfile=app.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "gsap": "^3.15.0",
    "three": "^0.186.1"
  }
}
